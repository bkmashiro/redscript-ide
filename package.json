{
  "name": "redscript-ide",
  "version": "0.1.0",
  "description": "RedScript online IDE — compile to Minecraft datapacks in the browser",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx serve public -p 3000",
    "deploy": "node build.mjs && npx wrangler pages deploy public --project-name redscript-ide"
  },
  "dependencies": {
    "redscript-mc": "^1.2.6"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
